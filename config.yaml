# TODO: Currently unused, this file should replace src/config.py in the future

raw_audio_dir: data/raw_audio
cleaned_dir: data/cleaned_recordings
clips_dir: data/processed_clips
features_dir: data/features
models_dir: models
sample_rate: 16000
clip_duration: 3.0
min_clip_energy: 0.01
n_mfcc: 13
test_size: 0.2
random_state: 42
